name: vayug
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 3.6.3+61

environment:
  sdk: '>=3.5.0 <4.0.0'

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.2
  file_picker: ^10.1.9
  dio: ^5.9.1
  http: ^1.1.0  # Kept temporarily for backward compatibility during migration
  crypto: ^3.0.3
  shared_preferences: ^2.2.2
  share_plus: ^11.0.0
  android_intent_plus: ^5.0.0
  url_launcher: ^6.2.5
  app_links: ^6.3.3
  path_provider: ^2.1.5
  path: ^1.9.0
  permission_handler: ^11.0.1
  cached_network_image: ^3.3.0
  google_sign_in: ^6.1.6
  provider: ^6.1.2
  flutter_riverpod: ^2.5.1
  riverpod_annotation: ^2.3.5
  flutter_screenutil: ^5.9.3 
  # Debug-only device and accessibility viewport simulator. Disabled in release.
  device_preview: 1.2.0
  image_picker: ^1.0.4
  video_compress: ^3.1.4
  # **NEW: Image processing and cropping**
  image: ^4.1.7
  crop_your_image: ^1.1.0
  wakelock_plus: ^1.4.0
  # Video player dependencies - keep only essential ones
  video_player: ^2.8.1
  video_player_platform_interface: ^6.2.0
  video_player_android: ^2.3.7
  video_player_avfoundation: ^2.3.7
  # **NEW: Advanced video optimization package**
  video_thumbnail: ^0.5.3
  like_button: ^2.0.5
  http_parser: ^4.0.2

  visibility_detector: ^0.4.0+2
  in_app_update: ^4.2.1
  in_app_review: ^2.0.12


  jwt_decoder: ^2.0.1
  flutter_cache_manager: ^3.3.1
  connectivity_plus: ^6.0.5
  device_info_plus: ^12.3.0  # Downgraded for Dart SDK 3.5.0 compatibility
  package_info_plus: ^9.0.0
  # **NEW: Hive for Instant Data Loading**
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  geolocator: ^13.0.2
  geocoding: ^3.0.0
  firebase_messaging: ^15.1.3
  firebase_core: ^3.6.0
  flutter_local_notifications: ^17.2.2
  shimmer: ^3.0.0
  firebase_performance: ^0.10.1+10
  flutter_secure_storage: ^10.0.0
  google_fonts: ^8.0.0
  # 3.10+ changed the Android plugin's Kotlin/Gradle integration. Keep the
  # last known-good release until all Android plugins migrate together.
  photo_manager: 3.9.0
  screen_brightness: ^2.1.7
  flutter_volume_controller: ^1.3.4
  webview_flutter: ^4.13.1
  talker: ^4.5.0
  talker_flutter: ^4.5.0
  system_info2: ^4.1.0
  hugeicons: ^1.1.2
  vibration: ^3.1.7
  firebase_crashlytics: ^4.3.10
  ffmpeg_kit_flutter_new: ^4.6.2
  google_generative_ai: ^0.4.7
  gal: ^2.3.2
  # **NEW: E2EE (End-to-End Encryption) dependencies**
  pointycastle: ^3.9.1
  shelf: ^1.4.2
  purchases_flutter: ^10.7.0


dev_dependencies:
  flutter_launcher_icons: ^0.14.3
  flutter_lints: ^2.0.0
  riverpod_generator: ^2.4.0
  build_runner: ^2.4.9
  riverpod_lint: ^2.3.10
  integration_test:
    sdk: flutter
  mocktail: ^1.0.4
  flutter_test:
    sdk: flutter

flutter_launcher_icons:
  android: true
  ios: true
  image_path: "assets/icons/app_icon.png"
  adaptive_icon_background: "assets/icons/app_icon_bg.png"
  adaptive_icon_foreground: "assets/icons/app_icon_foreground.png"
# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - assets/icons/
    # - assets/images/

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package


# **NEW: Fluttertoast removed to fix compilation issues - using SnackBar instead**
# **NEW: No more fluttertoast dependency issues**
